import os, ipdb
import tensorflow as tf
import numpy as np
import configure as cfgi
from tfcommon import *

FLAGS = tf.app.flags.FLAGS


def inference(rgb_img, dep_img, rgb_model, dep_model, keep_prob, tag='deepfus'):
    return prob


def loss(prob, labels, tag='deepfus'):
    return loss
